node_modules/
.pnpm-store/

dist/
coverage/
*.tsbuildinfo
*.tgz

.env
.env.*

.DS_Store
.idea/

# native backend build artifacts
native/target/
native/*.node
# Regenerated by `napi build`, but napi cannot declare the addon's `Readback`
# return type, so the hand-written NativeModule in src/backends/native.ts is
# the real contract.
native/index.d.ts

# python bytecode caches
__pycache__/
*.pyc

# written by `pnpm example:char-rnn`, read back by `pnpm example:char-rnn:generate`
char-rnn.json
# fetched by `pnpm fetch:corpus`; 1.1 MB of text does not belong in git (data/essay.txt is committed)
examples/char-rnn/data/tiny-shakespeare.txt
