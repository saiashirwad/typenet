node_modules/
.pnpm-store/

dist/
coverage/
*.tsbuildinfo
*.tgz

.env
.env.*

.DS_Store
.idea/

# native backend build artifacts
native/target/
native/*.node
# Regenerated by `napi build`; napi cannot declare the addon's `Readback` return type, so the
# hand-written NativeModule in src/backends/native.ts is the real contract.
native/index.d.ts

# written by examples/char-rnn/train.ts, read back by examples/char-rnn/generate.ts
char-rnn.json
# fetched by `pnpm fetch:corpus`
examples/char-rnn/data/tiny-shakespeare.txt
