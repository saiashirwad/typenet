{
  // test/, with the unused-* flags off for its deliberately unreferenced `Expect<...>` aliases.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "include": ["test/**/*.ts"],
  "exclude": ["node_modules"]
}
